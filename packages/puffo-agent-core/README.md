# puffo-agent-core

Stdlib-only shared kernel for `puffo-agent`: path resolution, agent-id
validation, and profile (`# Soul`) parsing. No third-party deps. Consumed
by both the fat local agent (`puffo-agent`) and the slim cloud runtime
(`puffo-agent-cloud`).
